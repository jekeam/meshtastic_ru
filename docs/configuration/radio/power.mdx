---
id: power
title: "Настройки питания"
sidebar_label: "Питание"
description: "Настройки для продвинутых пользователей, которые хотят настроить питание на устройстве Meshtastic."
---

import Admonition from "@theme/Admonition";
import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import { Icon } from "@iconify/react";
import calculateADC from "/src/utils/calculateADC";

:::info Сначала Device Config
Питание — продвинутая настройка. Большинству пользователей достаточно выбрать роль в [Device Config](/docs/configuration/radio/device) для управления питанием и не менять эти параметры.
:::

Опции питания: Экономия энергии, Выключение после потери питания, Переопределение ADC Multiplier, Таймаут ожидания Bluetooth, Интервал Light Sleep, Минимальный интервал бодрствования и Адрес INA2xx батареи устройства. Питание config использует админ-сообщение `Config.Power` protobuf.

## Параметры питания {#power-config-values}

### Экономия энергии {#power-saving}

:::warning осторожно
Если включено, менять настройки можно, разбудив устройство кнопкой пользователя, перезагрузкой или через [admin channel](/docs/configuration/remote-admin/).
:::

При активации отключаются Bluetooth, Serial, WiFi и экран для экономии энергии. Полезно для устройств от слабых источников питания (например, солнечные панели). Какие роли включают это по умолчанию — см. раздел [Device Config](/docs/configuration/radio/device).

### Выключение после потери питания {#shutdown-after-losing-power}

Автоматическое выключение устройства через заданное время после потери питания.

Перед настройкой убедитесь, что плата правильно определяет подключение к внешнему питанию (иконка "🔌" в приложении). Иначе устройство может выключаться даже при наличии питания.
Многие устройства определяют питание корректно, но для некоторых потребуется настроить ADC Multiplier (ниже).

### Переопределение ADC Multiplier {#adc-multiplier-override}

Коэффициент делителя напряжения для пина батареи, например 3.20 (R1=100k, R2=220k).

Переопределяет ADC_MULTIPLIER, заданный в файле варианта устройства прошивки для расчёта напряжения батареи.

Должно быть числом с плавающей точкой от 2 до 6.

#### Калибровка ([Источник](https://wiki.uniteng.com/en/meshtastic/nano-g1-explorer#calibration-process)) {#calibration-process-attribution}

1. Установите аккумулятор.
2. Полностью зарядите батарею. Индикация зависит от устройства. В этот момент напряжение батареи должно быть 4.2V ±1%.
3. Введите значение "Battery Charge Percent", отображаемое на экране или в подключённом приложении, в калькулятор ниже.
4. Если "Battery Charge Percent" (например, B 3.82V 60%) не отображается, значит значение "Operative Adc Multiplier" слишком высокое. Уменьшайте "Operative Adc Multiplier" (рекомендуется по 0.1), пока на экране не появится "Battery Charge Percent". Введите текущее значение "Operative Adc Multiplier" в поле калькулятора. Также введите отображаемый "Battery Charge Percent".
5. Нажмите "Calculate", чтобы получить "Calculated New Operative Adc Multiplier", и установите его как новый "Operative Adc Multiplier" на устройстве.

<div class="no-print">
  <details>
    <summary>Калькулятор ADC</summary>
    <div>
      <Admonition type="info">
        <p>Этот способ калибровки лишь привязывает 4.2V к 100% заряда батареи и не учитывает возможную нелинейность ADC.</p>
      </Admonition>
      <table>
        <tr>
          <td> Процент заряда батареи: </td>
          <td>
            <input type="text" id="batteryChargePercent" defaultValue="65" />
          </td>
        </tr>
        <tr>
          <td>Текущий Adc Multiplier: </td>
          <td>
            <input type="text" id="operativeAdcMultiplier" defaultValue="2" />
          </td>
        </tr>
        <tr>
          <td>Новый расчётный Operative Adc Multiplier: </td>
          <td>
            <input
              type="text"
              id="newOperativeAdcMultiplier"
              value="2"
              disabled="disabled"
            />
          </td>
        </tr>
        <tr>
          <td></td>
          <td>
            <button
              className="button button--outline  button--lg cta--button"
              onClick={calculateADC}
            >
              Рассчитать
            </button>
          </td>
        </tr>
      </table>
      Значения ADC Multiplier по умолчанию:

      |  Устройство   | ADC Multiplier по умолчанию |
      | :-----------: | :--------------------------: |
      | chatter2 | 5.0 |
      | diy | 1.85 |
      | esp32-s3-pico | 3.1 |
      | heltec_v1 & heltec_v2 | 3.2 |
      | heltec_v3 & heltec_wsl_v3 | 4.9 |
      | heltec_wireless_paper | 2 |
      | heltec_wireless_tracker 1.0/1.1 | 4.9 |
      | heltec_T114 | 4.916 |
      | lora_isp4520 | 1.436 |
      | m5stack_coreink | 5 |
      | nano-g1-explorer | 2 |
      | nano-g2-ultra | 2 |
      | picomputer-s3 | 3.1 |
      | rak4631 | 1.73 |
      | rpipico | 3.1 |
      | rpipicow | 3.1 |
      | station-g1 | 6.45 |
      | station-g2 | 4 |
      | tlora_v2_1_16 | 2 |
      | tlora_v2_1_18 | 2.11 |
      | tlora_t3s3_v1 | 2.11 |
      | t-deck | 2.11 |
      | t-echo | 2 |

    </div>

  </details>
</div>

### Таймаут ожидания Bluetooth {#wait-bluetooth-interval}

Сколько ждать перед отключением BLE в режимах без Bluetooth.

`0` — значение по умолчанию (1 минута)

### Интервал Light Sleep {#light-sleep-interval}

:::info только ESP32
Настройка применима только к платам на ESP32, на nRF52/RP2040 не действует.
:::

В Light Sleep CPU приостановлен, LoRa включён, BLE выключен, GPS включён.

`0` — значение по умолчанию (пять минут)

### Минимальный интервал бодрствования {#minimum-wake-interval}

В режиме Light Sleep при получении пакетов по LoRa устройство просыпается, обрабатывает их и остаётся бодрствовать в режиме без Bluetooth в течение этого интервала.

`0` — значение по умолчанию (10 секунд)

### Адрес INA2xx батареи устройства {#device-battery-ina2xx-address}

Если на одном из I2C‑шин по указанному адресу автоматически обнаружен INA‑2XX, он будет использован как основной источник для считывания напряжения батареи устройства. Настройка игнорируется на устройствах с PMU (например, T-beams).

:::tip[Перевести шестнадцатеричный в десятичный]
Адреса I2C обычно задаются в шестнадцатеричном виде, для установки через клиенты Meshtastic их нужно преобразовать в десятичный. Например, 0x40 в десятичном — это 64.
:::

## Где доступна настройка питания {#power-config-client-availability}

<Tabs
  groupId="settings"
  defaultValue="apple"
  values={[
    {
      label: (
        <>
          <Icon icon="mdi:android" height="1.5rem" style={{ marginRight: "0.25rem" }} /> Android
        </>
      ),
      value: "android",
    },
    {
      label: (
        <>
          <Icon icon="mdi:apple" height="1.5rem" style={{ marginRight: "0.25rem" }} /> Apple
        </>
      ),
      value: "apple",
    },
    {
      label: (
        <>
          <Icon icon="mdi:terminal" height="1.5rem" style={{ marginRight: "0.25rem" }} /> CLI
        </>
      ),
      value: "cli",
    },
    {
      label: (
        <>
          <Icon icon="mdi:internet" height="1.5rem" style={{ marginRight: "0.25rem" }} /> Web
        </>
      ),
      value: "web",
    },
  ]}>

<TabItem value="android">

#### Android {#android}

:::info заметка

Опции Настройка Питания доступны на Android.

1. Откройте приложение Meshtastic
2. Перейдите: **Вертикальное многоточие (3 точки вверху справа) > Конфигурация радио > Power**

:::

</TabItem>
<TabItem value="apple">

#### Apple {#apple}

:::info заметка

Часть опций Питание доступна на iOS, iPadOS и macOS: Настройки > Настрйка Устройства > Питание.

:::

</TabItem>
<TabItem value="cli">

#### CLI {#cli}

:::info заметка

Все опции Питание доступны в python CLI.

:::

|              Настройка              |        Допустимые значения         |           По умолчанию            |
| :---------------------------------: | :--------------------------------: | :-------------------------------: |
|        power.is_power_saving        |          `true`, `false`           |               `false`             |
| power.on_battery_shutdown_after_secs |        `integer` (seconds)         |        По умолчанию `0` — выкл.   |
|   power.adc_multiplier_override     |    `2-4` (floating point value)    | `0` — использовать значения прошивки |
|      power.wait_bluetooth_secs      |        `integer` (seconds)         |     `0` — 1 минута                |
|            power.ls_secs            |        `integer` (seconds)         |     `0` — 5 минут                 |
|         power.min_wake_secs         |        `integer` (seconds)         |     `0` — 10 секунд               |
|  power.device_battery_ina_address   | `integer` (I2C address as decimal) |  `0` — адрес не задан             |

:::tip подсказка

Так как устройство перезагружается после каждой команды через CLI, при установке нескольких параметров одной секции лучше объединять их в одну команду.

```shell title="Пример:"
meshtastic --set power.is_power_saving true --set power.on_battery_shutdown_after_secs 120
```

:::

```shell title="Включить / выключить экономию энергии"
meshtastic --set power.is_power_saving true
meshtastic --set power.is_power_saving false
```

```shell title="Включить / выключить автоотключение после потери питания"
meshtastic --set power.on_battery_shutdown_after_secs 120
meshtastic --set power.on_battery_shutdown_after_secs 0
```

```shell title="Установить таймаут ожидания Bluetooth (0 по умолчанию = 60 секунд)"
meshtastic --set power.wait_bluetooth_secs 0
meshtastic --set power.wait_bluetooth_secs 120
```

```shell title="Установить Light Sleep по умолчанию (0 по умолчанию = 5 минут)"
meshtastic --set power.ls_secs 0
meshtastic --set power.ls_secs 120
```

```shell title="Установить минимальный интервал бодрствования (0 по умолчанию = 10 секунд)"
meshtastic --set power.min_wake_secs 0
meshtastic --set power.min_wake_secs 120
```

</TabItem>
<TabItem value="web">

#### Web {#web}

:::info заметка
Все опции питания доступны в Web UI.
:::

</TabItem>
</Tabs>
